{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "MyNews app",
            "url": "http://localhost:5878/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}